/*
 Hash Table:
 - An unordered data structure that stores data in key-value pairs.
 - Provides average time complexity of O(1) for insertion, search, and deletion.

 Concept:
 - Works like an array/vector but each element is accessed using a key instead of an index.
 - A hash function is used to convert data (key) into a hash value (index).
   Example: "abc" → 344 (index in table).

 Uses:
 - Commonly used in applications where fast lookups are required.
 - Widely applied in real-world scenarios such as password storage, caching, and databases.
*/


/*

CREATE HASH FUNC 

we will convert key into index

*/

