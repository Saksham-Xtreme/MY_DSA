/*
Binary Tree: An Introduction 🌳

A binary tree is a hierarchical data structure where each node has, at most, two children, typically referred to as the left child and the right child. It's used to organize data in a way that makes searching,
 insertion, and deletion efficient.

Binary Tree Terminology

Leaf Node: A leaf node is a node that has no children. It's located at the "end" of a 

    branch and represents a terminal point in the tree structure. A tree can have one or more leaf nodes.

Level: The level of a node refers to its depth in the tree. The root node is at level 0. Its children are at level 1, 
their children are at level 2, and so on. This concept is useful for understanding the tree's height and for certain 
traversal algorithms.

Subtree: A subtree is a smaller tree within the main tree. Any node in a binary tree can be the root of its 
own subtree, which includes all of its descendants (all the nodes below it). For example, the left child of the root and all of its 
descendants form the left subtree of the main tree.
*/